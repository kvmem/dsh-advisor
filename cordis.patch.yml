# Keep the existing row ID so user configuration overlays still apply.
- insert:
    - id: advisor
      name: dsh-super-advisor
      config:
        provider: !!js process.env.DSH_ADVISOR_PROVIDER ?? ''
        model: !!js process.env.DSH_ADVISOR_MODEL ?? ''
        maxInputBytes: 32768
        maxOutputTokens: 0
        maxOutputBytes: 0
        maxCallsPerTask: 8
        timeoutMs: 120000
